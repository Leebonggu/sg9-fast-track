# 신분증 사본 업로드 — 설정 가이드 (2026-05-29)

사전동의(신속통합동의서) 완료 세대를 대상으로 신분증 사본을 업로드받는 기능. 공동소유는 소유자별로 N개 업로드. 코드는 모두 구현·빌드 통과 상태이며, **아래 수동 설정**을 마쳐야 실제로 동작한다.

---

## ✅ 내가 해야 할 일 (Step by Step)

코드(개발)는 끝났음. 아래는 **사람이 직접** 해야 하는 설정·배포 작업. 위에서 아래 순서대로.

### STEP 1 — 비공개 Drive 폴더 만들기
- [ ] 1-1. **PDF 웹앱을 배포했던 그 구글 계정**으로 로그인 (서비스 계정 아님, 사람 계정)
- [ ] 1-2. 구글 드라이브에서 새 폴더 생성 — 이름 예: `신분증_사본_제출`
- [ ] 1-3. 그 폴더는 **공유하지 않고 비공개로 둔다** (절대 "링크 있는 모든 사용자" 금지)
- [ ] 1-4. 폴더 열고 주소창 URL에서 폴더ID 복사
  - `https://drive.google.com/drive/folders/`**`여기가_폴더ID`**

### STEP 2 — 폴더ID를 환경변수에 넣기
- [ ] 2-1. `web/.env.local` 파일 열어 `ID_UPLOAD_FOLDER_ID=` 뒤에 복사한 폴더ID 붙여넣기 (지금은 비어 있음)
- [ ] 2-2. Vercel → web 프로젝트 → Settings → Environment Variables 에 아래 2개 추가
  - `ID_UPLOAD_FOLDER_ID` = (1-4의 폴더ID)
  - `ID_UPLOAD_SECRET` = `f24ee28b23760d1e2fcfd3ac053235a99a1ccfcd7f9807a7`

### STEP 3 — Apps Script 코드 교체
- [ ] 3-1. 이 웹앱의 Apps Script 편집기 열기 (PDF 만들 때 쓰던 그 프로젝트)
  - URL: `https://script.google.com/macros/s/AKfycbwPopnnhPhF2_NFj8L41wAtiQqtDkJIdDr_tQJNhi-0slGDoM7vxAPFNWNT5Sig0LXm/exec`
- [ ] 3-2. 기존 `.gs` 파일 내용을 **[4. 붙여넣기용 전체 코드]** 로 통째로 교체 후 저장(💾)

### STEP 4 — secret 등록 (1회)
- [ ] 4-1. Apps Script 편집기에서 맨 아래에 임시 함수 추가:
  ```javascript
  function _setupIdSecret() {
    setIdUploadSecret('f24ee28b23760d1e2fcfd3ac053235a99a1ccfcd7f9807a7');
  }
  ```
- [ ] 4-2. 상단 함수 선택 드롭다운에서 `_setupIdSecret` 고르고 ▶ 실행 (권한 요청 뜨면 허용)
- [ ] 4-3. 실행 로그에 "신분증 업로드 secret 설정 완료" 보이면 OK. 임시 함수는 지워도 됨

### STEP 5 — 웹앱 재배포 (URL 유지)
- [ ] 5-1. 배포 → **배포 관리** 클릭
- [ ] 5-2. 기존 배포 항목의 ✏️(편집) → **버전: 새 버전** 선택 → **배포**
  - ⚠️ "새 배포"로 만들면 URL이 바뀌니 금지. 반드시 **기존 배포의 새 버전**

### STEP 6 — Vercel 재배포
- [ ] 6-1. STEP 2-2에서 환경변수 추가했으면 Vercel에서 재배포(Redeploy) 1회 (변경 반영)

### STEP 7 — 동작 확인
- [ ] 7-1. 사전동의 **완료** 세대(동/호/등기 성명)로 `/check-submission` 인증 → "신분증 사본 제출" 보임
- [ ] 7-2. 동의 체크 → 소유자별 촬영/업로드 → Drive 폴더에 파일 생기는지, `신분증업로드` 시트에 행 생기는지 확인
- [ ] 7-3. `/unified` 로그인 → 세대 클릭 → 모달에서 신분증 썸네일/원본/폐기/인쇄 확인
- [ ] 7-4. 사전동의 **미완료** 세대는 업로드 섹션이 안 보이는지 확인

> 막히면: 업로드 시 "인증 실패(secret 불일치)" → STEP 4 secret 불일치 / "환경변수 … 설정되지 않았습니다" → STEP 2 폴더ID 누락 / 사진은 올라가는데 시트 기록 없음 → OWNER_SPREADSHEET_ID 권한 확인.

---

## 0. 동작 구조 (요약)

```
주민: /check-submission → 동/호/이름 인증 → (사전동의 완료 시) 소유자별 신분증 업로드
  → Next.js /api/upload-id (토큰검증·사전동의확인·소유자검증)
  → Apps Script 웹앱(idUpload, 사용자계정 권한) → 비공개 Drive 폴더 저장
  → OWNER_SPREADSHEET_ID "신분증업로드" 시트 기록

관리자: /unified 행 클릭 → 모달에서 소유자별 신분증 썸네일 열람 / 원본보기 / 폐기
  → /unified/id-print 에서 세대별 일괄 인쇄
  (이미지는 /api/upload-id/image 프록시, APP_PASSWORD 보호)
```

서비스 계정은 Drive 파일을 소유할 수 없어(`storageQuotaExceeded`), 기존 PDF와 동일하게 **Apps Script 웹앱을 다리로** 사용한다.

---

## 1. 비공개 Drive 폴더 만들기 → `ID_UPLOAD_FOLDER_ID`

1. `SURVEY_WEBAPP_URL` 웹앱을 배포한 **그 구글 계정**으로 구글 드라이브에서 새 폴더 생성 (예: `신분증_사본_제출`).
2. 폴더는 **비공개 유지** — "링크가 있는 모든 사용자"로 공유하지 말 것. (운영진 추가 열람이 필요하면 해당 구글 계정만 개별 공유)
3. 폴더 URL `https://drive.google.com/drive/folders/【이 부분이 폴더ID】` 에서 ID 복사.
4. 입력 위치:
   - 로컬: `web/.env.local` 의 `ID_UPLOAD_FOLDER_ID=` (현재 빈 값)
   - 배포: Vercel → 프로젝트(web) → Settings → Environment Variables 에 `ID_UPLOAD_FOLDER_ID` 추가

---

## 2. Apps Script 재배포

`SURVEY_WEBAPP_URL` 이 가리키는 Apps Script 프로젝트에 신분증 처리 코드가 추가됐다.

대상 웹앱 URL:
```
https://script.google.com/macros/s/AKfycbwPopnnhPhF2_NFj8L41wAtiQqtDkJIdDr_tQJNhi-0slGDoM7vxAPFNWNT5Sig0LXm/exec
```

1. 위 웹앱의 Apps Script 편집기 열기 (PDF 생성용으로 쓰던 그 프로젝트).
2. 해당 `.gs` 파일 내용을 **아래 [4. 붙여넣기용 전체 코드]** 로 전체 교체.
3. 저장.
4. **배포 → 배포 관리 → (기존 배포) 편집 → 버전: 새 버전 → 배포** (URL은 그대로 유지됨).
   - ⚠️ "새 배포"로 다시 만들면 URL이 바뀐다. 반드시 **기존 배포의 새 버전**으로 갱신할 것.

---

## 3. 웹앱에 secret 설정 (1회)

Next.js ↔ 웹앱 공유 비밀값. `.env.local` 의 `ID_UPLOAD_SECRET` 값:

```
f24ee28b23760d1e2fcfd3ac053235a99a1ccfcd7f9807a7
```

1. Apps Script 편집기 상단 함수 선택을 `setIdUploadSecret` 로 두고 한 번 실행하려면, 임시로 아래를 추가 실행하거나 콘솔에서 호출:
   - 편집기에서 함수 `setIdUploadSecret` 는 인자가 필요하므로, 임시 함수를 만들어 실행하는 게 간단하다:
     ```javascript
     function _setupIdSecret() {
       setIdUploadSecret('f24ee28b23760d1e2fcfd3ac053235a99a1ccfcd7f9807a7');
     }
     ```
   - `_setupIdSecret` 실행 → 실행 후 이 임시 함수는 지워도 됨 (값은 Script Properties에 저장됨).
2. Vercel 환경변수에도 동일하게 `ID_UPLOAD_SECRET` = 위 값 추가.

> 보안상 secret 값을 바꾸고 싶으면: `.env.local`/Vercel 값과 `setIdUploadSecret(...)` 값을 **항상 동일하게** 유지.

---

## 4. 붙여넣기용 전체 코드 (`survey_generic_webapp.gs`)

> 저장소 경로: `src/apps-script/survey_generic_webapp.gs` 와 동일. 아래를 그대로 Apps Script에 붙여넣으면 된다.

```javascript
/**
 * 범용 설문지 PDF 생성 웹앱 (+ 신분증 사본 업로드)
 */

function doPost(e) {
  try {
    var data = JSON.parse(e.postData.contents);

    // ── 신분증 사본 업로드/삭제/조회 (PDF 생성과 별개 분기) ──
    if (data.mode === 'idUpload' || data.mode === 'idDelete' || data.mode === 'idFetch') {
      return jsonResponse(handleIdMode(data));
    }

    if (!data.templateDocId) {
      return jsonResponse({ error: 'templateDocId가 필요합니다.' });
    }
    if (!data.pdfFolderId) {
      return jsonResponse({ error: 'pdfFolderId가 필요합니다.' });
    }

    var result;

    if (data.mode === 'blank') {
      result = generateBlankPdf(data);
    } else if (data.mode === 'single') {
      result = generateCheckedPdf(data);
    } else {
      return jsonResponse({ error: 'mode는 blank 또는 single이어야 합니다.' });
    }

    return jsonResponse(result);
  } catch (err) {
    return jsonResponse({ error: err.message });
  }
}

function doGet() {
  return jsonResponse({ status: 'ok', message: '범용 설문지 PDF 생성 웹앱' });
}

/**
 * 빈 설문지 PDF 생성
 */
function generateBlankPdf(data) {
  var templateDocId = data.templateDocId;
  var pdfFolderId = data.pdfFolderId;
  var basicInfoFields = data.basicInfoFields || [];
  var questions = data.questions || [];

  var copy = DriveApp.getFileById(templateDocId).makeCopy(
    '설문지_빈양식',
    DriveApp.getFolderById(pdfFolderId)
  );
  var copiedDocId = copy.getId();

  try {
    var doc = DocumentApp.openById(copiedDocId);
    var body = doc.getBody();

    for (var i = 0; i < basicInfoFields.length; i++) {
      var field = basicInfoFields[i];
      var tag = '\\{\\{' + escapeRegex(field.key) + '\\}\\}';
      body.replaceText(tag, '________________');
    }

    for (var i = 0; i < questions.length; i++) {
      var q = questions[i];
      var options = q.options || [];
      for (var j = 0; j < options.length; j++) {
        var optionTag = '\\{\\{' + escapeRegex(q.id) + '_' + escapeRegex(options[j]) + '\\}\\}';
        body.replaceText('☐' + optionTag, '☐');
      }
    }

    doc.saveAndClose();

    var pdfBlob = copy.getAs('application/pdf');
    pdfBlob.setName('설문지_빈양식.pdf');
    var pdfFile = DriveApp.getFolderById(pdfFolderId).createFile(pdfBlob);

    return { success: true, link: pdfFile.getUrl() };
  } finally {
    DriveApp.getFileById(copiedDocId).setTrashed(true);
  }
}

/**
 * 응답 기반 체크된 설문지 PDF 생성
 */
function generateCheckedPdf(data) {
  var templateDocId = data.templateDocId;
  var pdfFolderId = data.pdfFolderId;
  var basicInfoFields = data.basicInfoFields || [];
  var basicInfo = data.basicInfo || {};
  var questions = data.questions || [];
  var answers = data.answers || {};

  var dong = basicInfo.dong || '';
  var ho = basicInfo.ho || '';
  var name = basicInfo.name || '';
  var fileName = '설문지_' + dong + '_' + ho + '호_' + name;

  var copy = DriveApp.getFileById(templateDocId).makeCopy(
    fileName,
    DriveApp.getFolderById(pdfFolderId)
  );
  var copiedDocId = copy.getId();

  try {
    var doc = DocumentApp.openById(copiedDocId);
    var body = doc.getBody();

    for (var i = 0; i < basicInfoFields.length; i++) {
      var field = basicInfoFields[i];
      var value = basicInfo[field.key] || '';
      var tag = '\\{\\{' + escapeRegex(field.key) + '\\}\\}';
      body.replaceText(tag, value);
    }

    for (var i = 0; i < questions.length; i++) {
      var q = questions[i];
      var selected = answers[q.id] || '';
      var options = q.options || [];
      for (var j = 0; j < options.length; j++) {
        var optionTag = '\\{\\{' + escapeRegex(q.id) + '_' + escapeRegex(options[j]) + '\\}\\}';
        if (options[j] === selected) {
          body.replaceText('☐' + optionTag, '☑');
        } else {
          body.replaceText('☐' + optionTag, '☐');
        }
      }
    }

    doc.saveAndClose();

    var pdfBlob = copy.getAs('application/pdf');
    pdfBlob.setName(fileName + '.pdf');
    var pdfFile = DriveApp.getFolderById(pdfFolderId).createFile(pdfBlob);

    return { success: true, link: pdfFile.getUrl() };
  } finally {
    DriveApp.getFileById(copiedDocId).setTrashed(true);
  }
}

// ============================================================
// 신분증 사본 업로드/삭제/조회
//   - 비공개 폴더에 파일 저장 (별도 공유 설정 안 함 → 폴더 권한 그대로)
//   - 공유 secret(ID_UPLOAD_SECRET) 검증으로 무단 호출 차단
//   배포 전 1회: setIdUploadSecret('웹.env와_동일한_값') 실행
// ============================================================
function setIdUploadSecret(secret) {
  PropertiesService.getScriptProperties().setProperty('ID_UPLOAD_SECRET', secret);
  Logger.log('신분증 업로드 secret 설정 완료');
}

function checkIdSecret(data) {
  var stored = PropertiesService.getScriptProperties().getProperty('ID_UPLOAD_SECRET');
  return !!stored && data.secret === stored;
}

function handleIdMode(data) {
  if (!checkIdSecret(data)) {
    return { error: '인증 실패(secret 불일치)' };
  }

  if (data.mode === 'idUpload') {
    if (!data.folderId) return { error: 'folderId가 필요합니다.' };
    if (!data.base64) return { error: 'base64가 필요합니다.' };
    var bytes = Utilities.base64Decode(data.base64);
    var blob = Utilities.newBlob(bytes, data.mimeType || 'image/jpeg', data.fileName || 'id.jpg');
    var file = DriveApp.getFolderById(data.folderId).createFile(blob);
    return { success: true, fileId: file.getId(), link: file.getUrl() };
  }

  if (data.mode === 'idDelete') {
    if (!data.fileId) return { error: 'fileId가 필요합니다.' };
    DriveApp.getFileById(data.fileId).setTrashed(true);
    return { success: true };
  }

  if (data.mode === 'idFetch') {
    if (!data.fileId) return { error: 'fileId가 필요합니다.' };
    var f = DriveApp.getFileById(data.fileId);
    var b = f.getBlob();
    return {
      success: true,
      mimeType: b.getContentType(),
      base64: Utilities.base64Encode(b.getBytes())
    };
  }

  return { error: 'unknown id mode' };
}

function escapeRegex(str) {
  return String(str).replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}

function jsonResponse(obj) {
  return ContentService.createTextOutput(JSON.stringify(obj))
    .setMimeType(ContentService.MimeType.JSON);
}
```

---

## 5. 동작 검증 체크리스트

설정(1~3) 완료 후:

1. **빈 폴더 ID/secret 누락 시**: 업로드하면 "환경변수 … 설정되지 않았습니다" 또는 "인증 실패" → 설정 확인.
2. **주민 업로드**:
   - 사전동의 **완료** 세대(동/호/등기 성명)로 `/check-submission` 인증 → 결과 화면에 "신분증 사본 제출" 노출.
   - 동의 체크 전에는 업로드 버튼 비활성.
   - 단독소유 1슬롯 / 공동소유 N슬롯 확인.
   - 촬영/선택 → Drive 폴더에 파일 생성 + `신분증업로드` 시트에 행 기록. 재업로드 시 같은 행 갱신.
   - 사전동의 **미완료** 세대 → 업로드 섹션 미노출 (직접 호출 시 403).
3. **관리자**:
   - `/unified` 로그인(APP_PASSWORD) → 세대 행 클릭 → 모달에 소유자별 신분증 썸네일.
   - "원본" 링크로 Drive 열람, "폐기"로 삭제(시트 상태 '파기' + Drive 휴지통).
   - "인쇄용 보기" → `/unified/id-print?dong=&ho=` 에서 이미지 로드 후 자동 인쇄창.

---

## 6. 변경된/추가된 파일

| 구분 | 경로 |
|------|------|
| Apps Script | `src/apps-script/survey_generic_webapp.gs` (id 모드 + secret) |
| 신규 lib | `web/src/lib/id-upload.ts` |
| 신규 API | `web/src/app/api/upload-id/route.ts` (POST/GET/DELETE) |
| 신규 API | `web/src/app/api/upload-id/image/route.ts` (관리자 이미지 프록시) |
| 신규 컴포넌트 | `web/src/components/IdUploadSection.tsx` (주민 업로드) |
| 신규 컴포넌트 | `web/src/components/unified/IdUploadAdmin.tsx` (관리자 열람/폐기) |
| 신규 페이지 | `web/src/app/unified/id-print/page.tsx` (인쇄) |
| 수정 | `web/src/app/check-submission/result/page.tsx` (업로드 섹션 연결) |
| 수정 | `web/src/components/unified/EditRowModal.tsx` (신분증 섹션) |
| 수정 | `web/src/components/AdminLayout.tsx` (로그인 시 pw 보관) |
| 수정 | `web/src/lib/kakao-verify-log.ts` (로그 결과 타입 확장) |
| 수정 | `web/.env.local`, `CLAUDE.md` (환경변수 2개) |

## 7. 개인정보 처리 메모

- 신분증 **원본(가림 없음)** 수집 — 목적: 소유자 본인확인 및 원본(등기) 정보 갱신. 주민번호 전체 포함.
- 저장 폴더 비공개 + secret + APP_PASSWORD 프록시로 접근 통제. 시트엔 링크/파일ID만 저장(이미지 원본 미저장).
- **자동 파기 없음** → 갱신 완료 후 운영진이 수동 폐기.
- ⚠️ 주민번호 전체 보관은 개인정보보호법상 리스크가 큼(법 제24조의2). 보관 최소화·접근 통제·완료 후 즉시 폐기 권장.
